<view
    class="mx-font icon mx_{{name}}"
    style="font-size: {{size}}rpx;{{stroke ? ('color: transparent; -webkit-text-stroke: ' + stroke + ' ' + (color || '#A02731') + ';') : (color ? ('color: ' + color + ';') : '')}}"
/>
