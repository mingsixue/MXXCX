<mx-nav theme="light" title="底部导航 · 分类" />
<view class="page">
    <view class="intro">
        <view class="title">分类</view>
        <view class="desc">这里演示分类 Tab 内容。</view>
    </view>
    <view class="list-panel">
        <mx-list-basic
            wx:for="{{cates}}"
            wx:key="*this"
            label="{{item}}"
            arrows="{{true}}"
            showLine="{{index < cates.length - 1}}"
        />
    </view>
</view>
<mx-menu options="{{menus}}" current="{{1}}" selectColor="#292B2F" />
<debug-panel />
