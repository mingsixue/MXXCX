<mx-nav theme="light" title="食品管理" />

<view class="menu-page">
    <view class="menu-list">
        <view
            class="menu-card"
            hover-class="menu-card--hover"
            wx:for="{{entries}}"
            wx:key="path"
            data-path="{{item.path}}"
            bindtap="handleEntry"
        >
            <view class="menu-icon {{item.expired ? 'menu-icon--expired' : ''}}">{{item.mark}}</view>
            <view class="menu-main">
                <view class="menu-title">{{item.title}}</view>
                <view class="menu-desc">{{item.desc}}</view>
            </view>
            <view class="menu-arrow">›</view>
        </view>
    </view>
    <mx-safe-area />
</view>
<debug-panel />
